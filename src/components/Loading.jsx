import React from "react";
const styling = {
  textAlign: "center",
  fontSize: "18px",
  fontWeight: 600,
};
const Loading = () => {
  return <p style={styling}>Loading....</p>;
};

export default Loading;
